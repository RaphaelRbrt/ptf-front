apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ptf-front
  namespace: ptf-production
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`raphaelrbr.com`)
      kind: Rule
      services:
        - name: ptf-front
          port: 3000
      middlewares:
        - name: compress
        - name: security-headers
  tls:
    secretName: ptf-production-tls
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: compress
  namespace: ptf-production
spec:
  compress: {}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: security-headers
  namespace: ptf-production
spec:
  headers:
    frameDeny: true
    browserXssFilter: true
    contentTypeNosniff: true
    forceSTSHeader: true
    stsIncludeSubdomains: true
    stsPreload: true
    stsSeconds: 31536000
