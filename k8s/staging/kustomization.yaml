apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cvb-staging

resources:
  - deployment.yaml
  - ingress.yaml

images:
  - name: ghcr.io/raphaelrbrt/ptf-front
    newTag: staging

commonLabels:
  app: ptf-front
  environment: staging